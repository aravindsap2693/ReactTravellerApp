# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Dependency directories
node_modules/

# Vite build output
dist/

# Local environment variables
.env
.env.local
.env.*.local

# Editor directories and settings
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# System files
.DS_Store
Thumbs.db

# Package lockfiles (depending on the package manager)
# You may want to keep these files if you prefer deterministic builds, but you can ignore them if you don't.
# Uncomment the relevant one:
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Temporary files
*.swp
*.tmp

# Optional: coverage files (if you're using coverage tools like Jest or Cypress)
coverage/

# Optional: Storybook build output
.storybook-out/
storybook-static/

# Optional: local testing certificates (if you have any)
localhost.*

# Optional: testing tool results
.cypress/
.cypress_cache/
playwright-report/
test-results/